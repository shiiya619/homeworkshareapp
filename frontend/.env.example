# ローカル開発では空のままでOK（devユーザーで動作）。
# 本番(Render)ではLINE Developersで取得したLIFF IDを設定する（例: 1234567890-abcdefgh）。
VITE_LIFF_ID=

# バックエンドAPIのベースURL。ローカルはlocalhost、本番はRenderのAPI URL + /api。
VITE_API_BASE_URL=http://localhost:3000/api
